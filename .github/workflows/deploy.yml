name: Deploy to GitHub Pages
on:
  push:
    branches: [main]  

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cleanup
        run: |
          rm -f app.py forum.db

      - name: Prepare HTML Files
        run: |
          cp templates/*.html .
